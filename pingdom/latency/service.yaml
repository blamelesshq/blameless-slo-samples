apiVersion: blameless/v1alpha
kind: Service

metadata:
  name: Service-pingdom-latency  # unique name of the Service (group of SLIs)
  displayName: service.yaml found at blameless-slo-samples/pingdom/latency   # optional

spec:
  description: "source: http://blamelesshq/blameless-slo-sample/pingdom/latency/service.yaml"
  # notes: string                 # optional
  owner: nicolas@blameless.com  # name of a user in your Blmameless account (e.g. user email)
